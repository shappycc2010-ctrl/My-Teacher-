import authRouter, { authMiddleware } from "./auth.js";
app.use("/", authRouter);

// protect dashboard / progress endpoints:
// example
app.get("/api/progress/:studentName", authMiddleware, (req, res) => { /* ... */ });
